---
import BaseLayout from "../../layouts/BaseLayout.astro";
import YouTubeEmbed from "../../components/YouTubeEmbed.astro";
import {
  videos,
  videoCategories as categories,
  formatDate,
} from "../../data/videos";
---

<BaseLayout
  title="Vídeo"
  description="Producciones audiovisuales de Sergi Ortega — The Churfer. Vídeo comercial para marcas, videoclips musicales y cobertura de eventos deportivos."
>
  <!-- ═══ PAGE HERO ═══ -->
  <section class="page-hero">
    <div class="page-hero__inner container">
      <span class="label reveal">Portfolio</span>
      <h1 class="reveal">Vídeo</h1>
      <p class="page-hero__desc reveal">
        Vídeo comercial para empresas, videoclips musicales y cobertura de
        eventos deportivos. Grabación y edición con un enfoque cinematográfico
        que da carácter a cada producción.
      </p>
      <div class="rule reveal"></div>
    </div>
  </section>

  <!-- ═══ FILTERS ═══ -->
  <nav class="filters container reveal" aria-label="Filtrar por tipo">
    {
      categories.map(({ id, label }) => (
        <button
          class="filters__btn"
          data-filter={id}
          aria-pressed={id === "all" ? "true" : "false"}
        >
          {label}
        </button>
      ))
    }
  </nav>

  <!-- ═══ VIDEO GALLERY ═══ -->
  <section class="video-gallery container">
    {
      videos.map((video, i) => (
        <a
          href={`/video/${video.slug}`}
          class={`video-card reveal reveal-delay-${(i % 3) + 1}`}
          data-category={video.category}
        >
          <div class="video-card__embed">
            <YouTubeEmbed videoId={video.youtubeId} title={video.title} />
          </div>
          <div class="video-card__info">
            <div class="video-card__meta">
              <span class="label">
                {categories.find((c) => c.id === video.category)?.label}
              </span>
              <time class="video-card__date" datetime={video.date}>
                {formatDate(video.date)}
              </time>
            </div>
            <h3 class="video-card__title">{video.title}</h3>
            <p class="video-card__desc">{video.description}</p>
            <span class="video-card__more">Ver proyecto →</span>
          </div>
        </a>
      ))
    }
  </section>
</BaseLayout>

<script>
  const btns = document.querySelectorAll<HTMLButtonElement>(".filters__btn");
  const items = document.querySelectorAll<HTMLElement>(".video-card");

  btns.forEach((btn) => {
    btn.addEventListener("click", () => {
      const filter = btn.dataset.filter;
      btns.forEach((b) => b.setAttribute("aria-pressed", "false"));
      btn.setAttribute("aria-pressed", "true");

      items.forEach((item) => {
        const match = filter === "all" || item.dataset.category === filter;
        item.style.display = match ? "" : "none";
      });
    });
  });
</script>

<style>
  /* ═══ PAGE HERO ═══ */
  .page-hero {
    padding-top: calc(var(--space-section) + 80px);
    padding-bottom: var(--space-xl);
    border-bottom: 1px solid var(--color-border);
  }

  .page-hero h1 {
    margin-top: var(--space-xs);
  }

  .page-hero__desc {
    color: var(--color-text-muted);
    margin-top: var(--space-sm);
    max-width: 55ch;
    line-height: 1.8;
  }

  .page-hero .rule {
    margin-top: var(--space-md);
  }

  /* ═══ FILTERS ═══ */
  .filters {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5em;
    padding-block: var(--space-lg);
  }

  .filters__btn {
    font-family: var(--font-body);
    font-size: var(--text-xs);
    font-weight: 500;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    padding: 0.65em 1.5em;
    color: var(--color-text-dim);
    background: rgba(255, 255, 255, 0.03);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    border: 1px solid rgba(255, 255, 255, 0.06);
    border-radius: 100px;
    cursor: pointer;
    transition:
      color 0.3s cubic-bezier(0.22, 1, 0.36, 1),
      background 0.3s cubic-bezier(0.22, 1, 0.36, 1),
      border-color 0.3s cubic-bezier(0.22, 1, 0.36, 1),
      box-shadow 0.3s cubic-bezier(0.22, 1, 0.36, 1);
  }

  .filters__btn:hover {
    color: var(--color-text-muted);
    background: rgba(255, 255, 255, 0.06);
    border-color: rgba(255, 255, 255, 0.1);
  }

  .filters__btn:focus-visible {
    outline: 2px solid var(--color-sunset);
    outline-offset: 2px;
  }

  .filters__btn[aria-pressed="true"] {
    color: var(--color-text);
    background: rgba(212, 168, 83, 0.12);
    border-color: rgba(212, 168, 83, 0.3);
    box-shadow: 0 0 16px -4px rgba(212, 168, 83, 0.15);
  }

  /* ═══ VIDEO GALLERY ═══ */
  .video-gallery {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-lg);
    padding-bottom: var(--space-section);
  }

  .video-card {
    display: flex;
    flex-direction: column;
    border: 1px solid var(--color-border);
    text-decoration: none;
    color: inherit;
    transition:
      border-color var(--duration-base) var(--ease-out-expo),
      background var(--duration-base) var(--ease-out-expo);
  }

  .video-card:hover {
    border-color: var(--color-border-strong);
    background: var(--color-bg-raised);
  }

  .video-card__embed {
    border-bottom: 1px solid var(--color-border);
    pointer-events: none;
  }

  .video-card__info {
    padding: var(--space-md);
    display: flex;
    flex-direction: column;
    gap: var(--space-xs);
  }

  .video-card__meta {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .video-card__date {
    font-size: var(--text-xs);
    color: var(--color-text-dim);
    font-style: italic;
    font-family: var(--font-display);
  }

  .video-card__title {
    font-size: var(--text-xl);
    line-height: 1.2;
  }

  .video-card__desc {
    font-size: var(--text-sm);
    color: var(--color-text-muted);
    line-height: 1.65;
  }

  .video-card__more {
    display: inline-block;
    margin-top: auto;
    padding-top: var(--space-xs);
    font-size: var(--text-xs);
    font-weight: 500;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--color-sunset);
    opacity: 0;
    transform: translateY(4px);
    transition:
      opacity var(--duration-fast) var(--ease-out-quart),
      transform var(--duration-fast) var(--ease-out-quart);
  }

  .video-card:hover .video-card__more {
    opacity: 1;
    transform: translateY(0);
  }

  @media (max-width: 768px) {
    .video-gallery {
      grid-template-columns: 1fr;
    }
  }
</style>
